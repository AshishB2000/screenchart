# Claude Code — personal, machine-specific overrides (never commit)
.claude/settings.local.json

# OS junk
.DS_Store

# Node / Electron
node_modules/

# Build output
dist/
out/
build/

# Local config / secrets (never commit API keys)
.env
config.json

# Claude Code agent worktrees (transient)
.claude/worktrees/

<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Electron/V8 needs a writable+executable JIT heap. Under a hardened runtime
       these are required or the app crashes on launch. No Apple certificate is
       involved — this is a FREE ad-hoc signature; these entitlements just make it
       a COMPLETE seal (Info.plist bound, resources sealed) so macOS holds the
       Screen Recording (TCC) grant across relaunches. -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
  <key>com.apple.security.cs.allow-dyld-environment-variables</key>
  <true/>
</dict>
</plist>

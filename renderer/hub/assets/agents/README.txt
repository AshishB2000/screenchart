Drop full-color agent logo files here, named <agentId>.svg (preferred) or <agentId>.png.
Auto-discovered by src/icons.js and rendered as the agent's logo (falls back to a badge if absent).
Known agent ids include: antigravity, openai, gateway, grok, codex.
